% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vectors.R
\name{cosine_similarity}
\alias{cosine_similarity}
\title{Cosine similarity between two vectors or two matrices}
\usage{
cosine_similarity(a, b)
}
\arguments{
\item{a}{A numeric vector or matrix (rows = observations).}

\item{b}{A numeric vector or matrix (rows = observations) with the same
number of dimensions as \code{a}.}
}
\value{
\itemize{
\item A scalar when both \code{a} and \code{b} are vectors.
\item A matrix of size \verb{nrow(a) × nrow(b)} when both are matrices.
}
}
\description{
Computes cosine similarity. For L2-normalized providers (\code{nl}, \code{miniLM},
\verb{bge*}, \verb{mxbai*}, \verb{qwen3*}, \verb{nomic*}) cosine similarity equals the dot
product; the function normalizes anyway so unnormalized input is handled
correctly.  For \code{fdl} embeddings, consider calling \code{\link[=normalize_vectors]{normalize_vectors()}}
first.
}
\examples{
a <- c(1, 0, 0)
b <- c(0, 1, 0)
cosine_similarity(a, b)   # 0

cosine_similarity(a, a)   # 1
}
