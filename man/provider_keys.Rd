% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/providers.R
\name{provider_keys}
\alias{provider_keys}
\title{Known embedding provider keys}
\usage{
provider_keys()
}
\value{
A named character vector mapping provider key strings to short
model descriptions.
}
\description{
Returns the embedding provider identifiers used in the \code{provider} column of
the \code{embeddings} table.  Custom HuggingFace Hub models are stored with the
prefix \code{"custom:<hub-id>"} and are not enumerated here.
}
\details{
Normalization status:
\itemize{
\item \strong{L2-normalized} (cosine similarity = dot product): \code{nl}, \code{miniLM},
\code{bgeBase}, \code{bgeLarge}, \code{mxbaiEmbedLarge}, \code{qwen3Embedding},
\code{nomicTextV1_5}.
\item \strong{NOT normalized} (raw frequency counts): \code{fdl} — call
\code{\link[=normalize_vectors]{normalize_vectors()}} before computing cosine similarity.
}
}
\examples{
provider_keys()
names(provider_keys())   # just the key strings
}
