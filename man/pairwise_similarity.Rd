% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vectors.R
\name{pairwise_similarity}
\alias{pairwise_similarity}
\title{All-pairs cosine similarity matrix}
\usage{
pairwise_similarity(mat)
}
\arguments{
\item{mat}{A numeric matrix with one row per embedding chunk.}
}
\value{
A symmetric numeric matrix of size \verb{nrow(mat) × nrow(mat)} with
values in \verb{[-1, 1]}.
}
\description{
Computes the symmetric cosine similarity matrix for every pair of rows in
\code{mat}.  Rows are L2-normalized before multiplication, so raw or pre-
normalized input both work.
}
\examples{
\dontrun{
con <- open_corpus_store("results.sqlite")
emb <- read_embeddings(con, provider = "miniLM", part = "Introduction")
mat <- embedding_matrix(emb)
sim <- pairwise_similarity(mat)
rownames(sim) <- emb$title
hc  <- hclust(as.dist(1 - sim), method = "ward.D2")
plot(hc)
close_corpus_store(con)
}
}
