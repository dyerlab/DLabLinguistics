% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vectors.R
\name{embedding_matrix}
\alias{embedding_matrix}
\title{Convert a list-column of embedding vectors to a matrix}
\usage{
embedding_matrix(emb_df)
}
\arguments{
\item{emb_df}{A tibble returned by \code{\link[=read_embeddings]{read_embeddings()}}, containing a \code{vec}
list-column.}
}
\value{
A numeric matrix with \code{nrow(emb_df)} rows and \code{dimensions} columns.
}
\description{
Stacks the \code{vec} list-column from a tibble returned by \code{\link[=read_embeddings]{read_embeddings()}}
into a plain numeric matrix suitable for distance or similarity calculations.
}
\examples{
\dontrun{
con  <- open_corpus_store("results.sqlite")
emb  <- read_embeddings(con, provider = "miniLM", part = "Abstract")
mat  <- embedding_matrix(emb)   # n_chunks × 384
close_corpus_store(con)
}
}
