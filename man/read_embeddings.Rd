% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{read_embeddings}
\alias{read_embeddings}
\title{Read embeddings with optional filtering}
\usage{
read_embeddings(con, provider = NULL, part = NULL, granularity = NULL)
}
\arguments{
\item{con}{A DBI connection returned by \code{\link[=open_corpus_store]{open_corpus_store()}}.}

\item{provider}{Character or \code{NULL}. If supplied, only rows whose \code{provider}
matches this value are returned.  See \code{\link[=provider_keys]{provider_keys()}} for valid values.}

\item{part}{Character or \code{NULL}. If supplied, only rows whose \code{part} matches
this manuscript section are returned.  Valid values: \code{"Title"},
\code{"Abstract"}, \code{"Introduction"}, \code{"Methods"}, \code{"Results"},
\code{"Discussion"}, \code{"Other"}.}

\item{granularity}{Character or \code{NULL}. If supplied, only rows matching
\code{"section"} or \code{"paragraph"} are returned.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with all \code{embeddings} columns plus \code{title},
\code{doi} (from \code{documents}), and \code{vec} (deserialized numeric vector).
}
\description{
Queries the \code{embeddings} table (joined to \code{documents} for \code{title} and \code{doi})
and deserializes the binary \code{vector} BLOB column into a \code{vec} list-column of
numeric vectors.
}
\details{
\strong{FDL normalization:} \code{fdl} vectors are raw frequency counts and are \strong{not}
L2-normalized by the Swift library.  Call \code{\link[=normalize_vectors]{normalize_vectors()}} on the result
of \code{\link[=embedding_matrix]{embedding_matrix()}} before using \code{\link[=cosine_similarity]{cosine_similarity()}} or
\code{\link[=pairwise_similarity]{pairwise_similarity()}} with \code{fdl} data.
}
\examples{
\dontrun{
con   <- open_corpus_store("results.sqlite")
intro <- read_embeddings(con, provider = "miniLM", part = "Introduction")
mat   <- embedding_matrix(intro)
close_corpus_store(con)
}
}
